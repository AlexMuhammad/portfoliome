/* eslint-disable react/prop-types */
import React from 'react';

const Wrapper = ({ children }) => {
  return <div className="mx-auto px-[350px] pt-[75px] pb-[50px]"> {children}</div>;
};

export default Wrapper;
